{
  "name": "sass",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sass:build": "npm run clean && sass app/sass:public/stylesheets",
    "clean": "npx rimraf public/stylesheets -y",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sass": "^1.56.1"
  }
}
